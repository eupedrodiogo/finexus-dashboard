# Regras do Projeto

* Sempre que eu enviar um comando de apenas uma letra **"d"** nesta conversa, você deve interpretar como o comando explícito para executar o deploy do projeto (rodar `npm run build` seguido de `firebase deploy`).
