{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$(git status --porcelain -- . ':!.firebase' ':!dist')\" ]; then git add -A -- . ':!.firebase' ':!dist' && git commit -m 'Auto-commit from Claude Code session' && git push; fi",
            "statusMessage": "Checking for changes to push...",
            "timeout": 60,
            "async": true
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q 'git push' && (npm run build && firebase deploy --project meufinexus) || true",
            "statusMessage": "Deploying to Firebase after git push...",
            "timeout": 300,
            "async": true
          }
        ]
      }
    ]
  }
}
